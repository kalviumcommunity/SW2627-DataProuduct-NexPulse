Contains reusable Python scripts.

- `data_workflow.py` runs the full pipeline: text normalisation, type enforcement, deduplication, and output reporting.
- `text_cleaning.py` contains reusable string-cleaning helpers and shared text normalisation rules.
